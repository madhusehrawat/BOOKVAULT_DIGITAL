# Dependency directories
node_modules/
jspm_packages/
typings/

# Environment variables and secrets
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Debug logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS metadata
.DS_Store
Thumbs.db

# Build and production folders
dist/
build/

# ── UPLOADS ──────────────────────────────────────────────────────────────────
# Ignore ONLY the pdf subfolder (large files, not needed in git)
# Book cover images (jpg, jpeg, png, webp) MUST be tracked so they
# deploy correctly to Render — DO NOT ignore the uploads root folder.

# IDE / Editor settings
.vscode/
.idea/
*.swp
*.swo

# Temporary files
.tmp
.cache